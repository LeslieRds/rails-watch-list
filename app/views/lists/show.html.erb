<div class="container">
  <h1>show</h1>
  <br>
    <h2><%= @list.name %></h2>
<br>
<%= link_to "add a movie", new_list_bookmark_path(@list) %>
<%= link_to "Delete", list_bookmark_path(@list) %>
<br>
  <% @list.movies.each do |movie| %>
    <p><%= movie.title %></p>
    <p><img src="<%= movie.poster_url %>"></p>
    <p><%= movie.overview %></p>
  <% end %>
<br>
<%= link_to "Back", lists_path %>
